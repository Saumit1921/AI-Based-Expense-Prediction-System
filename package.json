{
  "name": "ai-based-expense-prediction-system",
  "version": "1.0.0",
  "description": "An AI-powered financial expense prediction platform",
  "main": "index.js",
  "scripts": {
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:ai": "cd ai-service && uvicorn app:app --host 127.0.0.1 --port 8000 --reload",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:ai\"",
    "prisma:generate": "cd backend && npx prisma generate",
    "prisma:migrate": "cd backend && npx prisma migrate dev",
    "prisma:seed": "cd backend && node prisma/seed.js",
    "generate-data": "python scripts/generateSampleData.py"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
